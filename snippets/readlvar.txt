{$CLEO .cs}
wait 1000

const
start = 0@
index = 1@
offset = 2@
mem = 3@
result = 4@
end

var
start : int
index : int
offset : int
mem : int
result : int // change
end

start = 0xA48960

// which @ number it has
index = 386

// this may not be accurate, but it gotta do
offset = index
offset *= 4

// you might as well figure it out yourself lol
mem = start
mem += offset

result = read_memory mem size 4 vp 0 // you can change 4 to 1, 4, or 8

while TIMERA < 4000
wait 0
use_text_commands 1
display_text_with_number 600.0 200.0 'NUMBER' result
use_text_commands 0
end

// duplicate and change some of the above to show more
// could make it a gosub if I know how

terminate_this_custom_script
